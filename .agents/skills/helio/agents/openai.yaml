interface:
  display_name: "Helio"
  short_description: "Repo-specific patterns and workflows for helio"
  default_prompt: "Use the helio repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true