# Copy to .env.production on the VPS. Generate every secret with:
# openssl rand -hex 32
POSTGRES_USER=helio
POSTGRES_PASSWORD=production-db-password
POSTGRES_DB=helio
JWT_ACCESS_SECRET=production-jwt-access-secret
JWT_REFRESH_SECRET=production-jwt-refresh-secret
INTERNAL_REQUEST_SECRET=production-internal-request-secret
PAYMENT_PROVIDER=mock
FRONTEND_URL=https://helio.example.com
WEBHOOK_BASE_URL=https://helio.example.com
