{
  "name": "@helio/worker",
  "version": "1.0.0",
  "private": true,
  "description": "Helio 异步任务进程（BullMQ 消费者，与 api 共用代码）",
  "scripts": {
    "dev": "tsx watch src/main.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/main.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@helio/api": "workspace:*",
    "bullmq": "^5.34.0",
    "ioredis": "^5.4.2",
    "dotenv": "^16.4.7"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "@types/node": "^22.10.2"
  }
}
